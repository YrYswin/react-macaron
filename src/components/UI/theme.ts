import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3498db", // Синий цвет
    },
    secondary: {
      main: "#e74c3c", // Красный цвет
    },
  },
});

export default theme;
